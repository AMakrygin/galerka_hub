import LocationsManager from "@/components/LocationsManager";

export default function LocationsPage() {
  return (
    <main style={{ padding: 24 }}>
      <h1>Локации хранения</h1>
      <LocationsManager />
    </main>
  );
}